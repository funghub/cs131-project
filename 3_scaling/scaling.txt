Instructions
-------------------------------------------
Keep the raw data in a GCS bucket and read it directly via gs://… — do not copy multi-GB files into your home dir.
Real data-engineering work: define a schema, transform, groupBy / window functions, joins, caching, optionally Spark SQL.
Scaling experiment: run the same Spark job on 1 worker, then 2, then 4 and record the runtime each time.
Delete your cluster (gcloud dataproc clusters delete …) when you're done for the day. Ephemeral clusters / gcloud dataproc jobs submit pyspark are cheaper than long-running ones.
Deliverable:

Scaling results on your final poster (Section title should be "Scaling")
Experimentation details should be recorded in ~/cs131/project/3_scaling/scaling.txt along with other pyspark code and results under ~/cs131/project/3_scaling/.

Experimental details
-------------------------------------------

First attempt was cancelled because 2 executors took over 30 mins, added .cache() in 2 places in scaling.py

exec3: first two attempts failed with COMPUTE_ENGINE/UNAVAILABLE (zone us-central1-c capacity exhaustion). Retried [in same region / in us-east1] and succeeded. 
--> results_7-31.txt

We realized executors != workers so we reran with workers to get correct timing and the correct clusters.

Only one worker had to be run on a single node instance compared to 2 and 4 workers. There was also issues with the default disk size per instance so it was hardcoded in as 100GB for the 4 worker run.

ERROR: (gcloud.dataproc.batches.submit.pyspark) Batch job is FAILED. Detail: Insufficient 'CPUS_ALL_REGIONS' quota. Requested 20.0, available 16.0. Your resource request exceeds your available quota. See https://cloud.google.com/compute/resource-usage. Use https://cloud.google.com/docs/quotas/view-manage#requesting_higher_quota to request additional quota.
Running auto diagnostics on the batch. It may take few minutes before diagnostics output is available. Please check diagnostics output by running 'gcloud dataproc batches describe' command.
ERROR: (gcloud.dataproc.clusters.create) INVALID_ARGUMENT: Insufficient 'DISKS_TOTAL_GB' quota. Requested 5000.0, available 4096.0. Your resource request exceeds your available quota. See https://cloud.google.com/compute/resource-usage. Use https://cloud.google.com/docs/quotas/view-manage#requesting_higher_quota to request additional quota.

Example of success for 2 workers--> results_2w.txt

Timings
-------------------------------------------
=== [1 worker] Runtime: 1693s ===

=== [2 workers] Runtime: 1555s ===

=== [4 workers] Runtime: 1489s ===